language: node_js
node_js:
  - 8

branches:
  only:
  - master

addons:
  firefox: "latest"

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

script:
  - yarn run test:single-run
